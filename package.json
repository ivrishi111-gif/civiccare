{
  "name": "civiccare",
  "version": "1.0.0",
  "private": true,
  "description": "CIVICCARE — simple citizen problem reporting app (React + Express + Supabase + Gemini AI)",
  "scripts": {
    "setup": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client"
  }
}
